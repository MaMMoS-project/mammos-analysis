Classes `ExtrinsicProperties`, `MaximumEnergyProductProperties` and `LinearSegmentProperties` in the `hysteresis` module are now EntityCollection subclasses, so they can be converted to pandas dataframes and stored in the mammos-entity file formats.
